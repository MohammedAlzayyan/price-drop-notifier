{
  "name": "price-drop-notifier",
  "version": "1.0.0",
  "private": true,
  "description": "Embeddable widget + Userscript + Express API for price-drop notifications on Amazon and eBay",
  "scripts": {
    "build": "npm run build:widget && npm run build:server",
    "build:widget": "cd widget && npm install && npm run build",
    "build:server": "cd server && npm install && npm run build",
    "start": "node server/dist/index.js",
    "dev": "npm run build && npm start"
  }
}
